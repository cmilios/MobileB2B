<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gr.logistic_i.logistic_i">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logistic_i_logo"
        android:label="Logistic-i"
        android:logo="@drawable/logistic_i_logo"
        android:roundIcon="@drawable/logistic_i_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenuActivity" />
        <activity android:name=".MostOrderedItems" />
        <activity android:name=".VoucherDetailsActivity" />
        <activity android:name=".ConfirmVoucher"></activity>
    </application>

</manifest>